import Vault from "./vault";

// describe("Vault", () => {
//     it("getData", async () => {
//         // given
//         const host = "",
//             engineName = `${process.env.VAULT_ENGINE_NAME}`,
//             roleId = `${process.env.VAULT_ROLE_ID}`,
//             secretId = `${process.env.VAULT_SECRET_ID}`,
//             secretPath = "";
//
//         // when
//         const vault = new Vault(host, engineName);
//         const result = await vault.getData(roleId, secretId, secretPath);
//
//         // then
//         expect(result).toEqual({ client_id: "", client_secret: "" });
//     });
// });
